"""Utility scripts (synthetic data generation, data-safety scan).

This is a package so the demo can ``import scripts.generate_synthetic_data``.
Each module is also runnable directly, e.g. ``python -m scripts.sanitize_scan``.
"""
