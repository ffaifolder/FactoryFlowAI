# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/

# Build artifacts produced by the demo
/ledger.json
/dashboard/index.html
/dashboard-app/index.html

# Local env / secrets — never commit real credentials
.env

# Local data-safety denylist — holds real names; never commit it
.sanitize_blocklist

# OS
.DS_Store
