# Publishes the synthetic demo dashboard to GitHub Pages.
# Enable Pages for the repo (Settings -> Pages -> Source: GitHub Actions).
name: Deploy demo dashboard

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run demo (builds both dashboards from synthetic data)
        run: python -m factoryflow.demo
      - name: Data-safety scan
        run: python -m scripts.sanitize_scan
      - name: Upload Pages artifact (Tabler dashboard + vendored assets)
        uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard-app

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
